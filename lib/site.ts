/** Presentation switches that were editor props on the Claude Design artboard. */
export const site = {
  /** Faint 16px grid behind the page. */
  showPixelGrid: true,
  /** Show text labels under the rail icons (icons only when false). */
  navLabels: true,
} as const;
